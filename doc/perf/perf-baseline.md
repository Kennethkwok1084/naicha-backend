# Week 1 压测基线记录

> 负责人：后端-李四  
> 压测环境：Staging K3s  
> 压测工具：Locust 2.x + wrk 4.2 + ws_runner.py  
> 最后更新：填写日期

---

## 压测配置

| 参数 | 值 | 说明 |
|------|-------|------|
| 并发用户数 | 100 / 200 / 500 | 分三档压测 |
| 持续时间 | 30s / 60s / 120s | 逐级增加 |
| 支付回调比例 | 0.3 | 避免挤占并发 |
| WebSocket 连接数 | 200 | 模拟商户监控 |
| 数据集规模 | 1000 订单 + 50 用户 + 20 商品 | 脱敏快照版本：填写 |

---

## GET /api/v1/menu（菜单接口）

### 100 并发

| 指标 | 值 | SLO 目标 | 达标 |
|------|-----|----------|------|
| 平均延迟 | 填写 ms | - | - |
| P50 | 填写 ms | - | - |
| P95 | 填写 ms | ≤ 40ms | ☐ |
| P99 | 填写 ms | ≤ 60ms | ☐ |
| QPS | 填写 | ≥ 200 | ☐ |
| 错误率 | 填写 % | < 0.1% | ☐ |

### 200 并发
（同上表结构）

### 500 并发
（同上表结构）

---

## POST /api/v1/orders（创建订单）

### 100 并发

| 指标 | 值 | SLO 目标 | 达标 |
|------|-----|----------|------|
| 平均延迟 | 填写 ms | - | - |
| P50 | 填写 ms | - | - |
| P95 | 填写 ms | ≤ 200ms | ☐ |
| P99 | 填写 ms | ≤ 300ms | ☐ |
| QPS | 填写 | ≥ 30 | ☐ |
| 错误率 | 填写 % | < 0.5% | ☐ |

### 200 并发
（同上表结构）

### 500 并发
（同上表结构）

---

## POST /api/v1/payments/notify/wechat（支付回调）

### 100 并发（× 0.3 概率）

| 指标 | 值 | SLO 目标 | 达标 |
|------|-----|----------|------|
| 平均延迟 | 填写 ms | - | - |
| P50 | 填写 ms | - | - |
| P95 | 填写 ms | ≤ 120ms | ☐ |
| P99 | 填写 ms | ≤ 180ms | ☐ |
| 成功率 | 填写 % | ≥ 99% | ☐ |
| 幂等测试 | 填写 | 重复回调不重复发券 | ☐ |

---

## WebSocket /ws/merchant（商户监控）

### 200 连接

| 指标 | 值 | SLO 目标 | 达标 |
|------|-----|----------|------|
| 连接成功率 | 填写 % | ≥ 99% | ☐ |
| 心跳延迟 P50 | 填写 ms | - | - |
| 心跳延迟 P95 | 填写 ms | ≤ 2000ms | ☐ |
| 心跳延迟 P99 | 填写 ms | ≤ 3000ms | ☐ |
| 超时次数 | 填写 | < 5 | ☐ |
| 订单广播数 | 填写 | - | - |
| 订单广播校验失败 | 填写 | 0 | ☐ |

---

## 瓶颈分析（如有性能不达标）

### 数据库慢查询（Top 3）

```sql
SELECT query, calls, total_time, mean_time 
FROM pg_stat_statements 
ORDER BY total_time DESC 
LIMIT 3;
```

| SQL 片段 | 调用次数 | 总耗时 (ms) | 平均耗时 (ms) | 优化措施 |
|----------|----------|-------------|---------------|----------|
| 填写 | 填写 | 填写 | 填写 | 填写（如：添加索引、优化 ORM） |

### 应用层监控

| 指标 | 观测值 | 说明 |
|------|--------|------|
| 应用 CPU 使用率 | 填写 % | Prometheus `rate(process_cpu_seconds_total)` |
| 应用内存使用 | 填写 MB | `process_resident_memory_bytes` |
| PgBouncer 连接池 | 填写 | `SHOW STATS;` 中 `cl_active` / `cl_waiting` |
| Redis 连接数 | 填写 | `INFO CLIENTS` |

---

## 优化措施记录

| 问题 | 优化措施 | 验证结果 | 日期 |
|------|----------|----------|------|
| 示例：`orders` 表全表扫描 | 添加复合索引 `(status, created_at)` | P95 从 280ms → 150ms | 2025-10-22 |
| 填写 | 填写 | 填写 | 填写 |

---

## 回归验证

优化后重新压测，确认 P95 降至 SLO 以下：

| 接口 | 优化前 P95 | 优化后 P95 | SLO | 达标 |
|------|------------|------------|-----|------|
| GET /menu | 填写 | 填写 | ≤ 40ms | ☐ |
| POST /orders | 填写 | 填写 | ≤ 200ms | ☐ |
| POST /payments/notify | 填写 | 填写 | ≤ 120ms | ☐ |

---

## 验收结论

- ☐ **全部 SLO 达标** → 进入 Week 2  
- ☐ **部分不达标** → 延期 2 天继续优化  
- ☐ **严重瓶颈** → 升级决策（调整 SLO / 延期 / 扩容）

**验收人签字**：__________ 日期：__________
